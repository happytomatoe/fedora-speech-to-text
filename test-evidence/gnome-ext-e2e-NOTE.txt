GNOME Extension E2E Tests — Not Available
==========================================

The gnome-ext-e2e-test recipe was removed from the justfile because
gnome-ext/e2e-test.sh does not exist on this branch (fix/gnome-ext-build).

The script existed on the feat/api-key-command-substitution branch (commit
f868c5b) but was never merged to the current branch.

The justfile recipe was orphaned — it referenced a script that was never
present on this branch.

To run e2e tests, either:
1. Merge feat/api-key-command-substitution into this branch, or
2. Recreate the e2e-test.sh script on this branch.
